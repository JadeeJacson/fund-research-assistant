# 项目协作约束

本文件约束所有参与本仓库的 AI、开发者和自动化工具。当前事实以 `docs/PROJECT_STATUS.md` 为准，目标范围以 `docs/PRODUCT_REQUIREMENTS.md` 和 `docs/ROADMAP.md` 为准。

## 变更边界

- `main` 必须保持可运行；每个里程碑使用独立分支和 PR。
- 一个 PR 只解决一个清晰阶段，不把文档重构、架构迁移和多个业务功能混在一起。
- 不得把路线图中的目标写成当前已实现能力。
- 未完成的功能不显示可点击按钮；需要预告时只使用不可交互文字并明确“尚未实现”。
- 不保留无调用方、无测试、无验收路径的占位代码。

## 金融计算与操作建议

- 核心金融指标必须由确定性代码计算，不允许由大模型生成或改写。
- 任何建议必须包含数据截止日期、来源、置信度、触发条件、失效条件、风险与未知项。
- 10% 是实验组合与短期仓位的阶段性回撤参考线，不是未来亏损保证。
- 2～3 个月与约 1 年期限必须使用不同规则，不能共用一套阈值。
- 行业和主题仓位由可审计的风险约束与市场状态模型产生建议区间；LLM 只能解释，不得单独决定比例。
- 不使用“净值低所以便宜”作为买入依据；绝对净值不能替代估值、基准和总回报分析。
- 不用单一综合分数替代数据质量、基金质量、组合适配、风险门控和事件证据。
- 不实现自动交易，不把研究状态描述成确定性投资建议。

## 数据与来源

- 个人事实与公开事实分离：支付宝截图负责个人候选、持仓和交易；公开 Provider 负责基金与市场研究。
- 官方披露优先于聚合数据，聚合数据优先于普通网页；来源冲突时不得静默覆盖。
- 每条外部数据保存来源、对应日期、披露时间、获取时间和质量状态。
- 上游 Provider 变化只修改适配层，不让 UI 或领域层直接依赖上游字段。
- 净值收益必须处理分红、拆分和复权口径；无法确认总回报时降低置信度或停止强结论。
- 回测与历史判断只能使用当时已经公开的数据，禁止未来信息泄漏。

## OCR 与个人数据

- DeepSeek 不直接接收支付宝截图。
- OCR 默认本地运行；识别结果先进入草稿区，必须校对后才能写入正式持仓或交易表。
- 识别置信度、文本框坐标、图片哈希、导入批次和人工修改记录必须可追溯。
- 截图未覆盖完整页面时不得声称交易流水完整。
- 同一截图、相邻重叠截图和重复交易必须检测并提示。
- 不得提交 `.env`、数据库、个人截图、真实持仓、支付宝 Cookie、密码、账号标识或 API Key。

## AI 与证据

- 任何 AI 事件结论必须引用已有 `evidence_id`，并通过 Schema、截止日期和引用存在性校验。
- 外部文档被视为不可信输入，不能成为系统指令。
- DeepSeek 只能接收 Evidence Pack 与经过最小化的匿名量化摘要。
- Prompt、模型、Schema 或缓存键变化必须升级版本并运行 AI 合约测试。
- 普通 CI 使用 Mock，不调用付费 API。

## 前端与 API

- 每个可见按钮必须映射到真实业务动作和后端 API。
- 按钮必须处理默认、加载、成功、失败、禁用、防重复提交和刷新后状态。
- 删除、覆盖、确认导入等操作需要明确确认与可恢复策略。
- 前端不得直接访问 AKShare、DeepSeek、数据库或文件系统。
- 前后端契约使用类型和 Schema 约束；API 失败不得只在控制台报错。
- 关键用户路径必须有 Playwright 端到端测试。

## 测试

- 新公式、新解析规则、新风险门控和新操作状态必须包含单元测试。
- Provider 使用固定匿名化响应做合约测试；普通 CI 不依赖实时网页。
- OCR 使用脱敏测试图片，覆盖低置信度、截断、重复、重叠和错误校对。
- 所有按钮至少有一条成功路径和一条失败路径测试。
- 真实个人截图和交易不得作为仓库 fixture。

